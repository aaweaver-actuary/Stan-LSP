# Stan Language Server for VS Code

This thin client launches `stan-language-server` over stdio for `.stan` files.
Install the server binary first or set `stanLsp.serverPath`. Set
`stanLsp.stancPath` to enable authoritative compiler diagnostics on save.
